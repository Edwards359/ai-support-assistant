# Optional: without this, /support/analyze uses a deterministic template reply
# (no network call). Category/priority classification never uses an LLM.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

APP_NAME=AI Support Assistant
